# API
fastapi>=0.104,<1.0
uvicorn[standard]>=0.24,<1.0
pydantic>=2.5,<3.0
python-multipart>=0.0.9

# Data processing
pandas>=2.1,<3.0

# Detection pipeline (pre-installed in container; skip locally if only running API)
ultralytics>=8.0,<9.0
opencv-python-headless>=4.8,<5.0
lap>=0.5.12

# Testing
pytest>=7.4,<9.0
httpx>=0.25,<1.0
pytest-asyncio>=0.21,<1.0
